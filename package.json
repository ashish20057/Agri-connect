{
  "name": "agri-connect-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "setup:python": "pip install -r Plant-Disease-Classifier-main/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
